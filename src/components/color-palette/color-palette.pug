include __text/color-palette__text
include __color-box/color-palette__color-box

mixin color-palette(title, subtitle, color, shadow)
  - var className = "color-palette__color-box" + color + shadow
  .color-palette
    +color-box(className)
    +text(title, subtitle)
    